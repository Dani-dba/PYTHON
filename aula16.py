'''lanche = 'hamburguer'
lanche = 'suco'
print(lanche)'''
#lanche = ('hamburguer','Suco', 'pizza', 'Pudim')
'''for c in lanche:
    print (c)'''

lanche = ('hamburguer', 'suco','pizza','pudim','batata frita')
# print(lanche[1]) #imprime suco
# print(lanche[2])#imprime pizza
# print(lanche[-2])#imprime pizza
# print(lanche[1:3])#imprime suco e pizza
# print(lanche[2:])#imprime pizza e pudim
# print(lanche[:2])#imprime hamburguer e suco
# print(lanche[-3:])#imprime suco pizza e pudim
# print(lanche)#imprime a tupla inteira
##tuplas são imutáveis
# lanche = 'refrigerante'
# for c in lanche: #imprime a frase com cada um dos itens da tupla.
#     print(f'Eu vou comer {c}')
# print(len(lanche)) #mostra quantos itens tem na tupla
# for cont in range (0, len(lanche)):
#     print (cont)
# for cont in range(0, (len(lanche))): #imprime a frase com cada um dos itens da tupla
#     print(f'Eu vou comer {lanche[cont]}')
# for cont in range (0, (len(lanche))): #imprime os itens e suas respectivas posições.
#     print(f'Eu vou comer {lanche[cont]} na posição {cont}')
# for pos, comida in enumerate(lanche): #mostra os itens da tupla na ordem e suas posições.
#     print(f'Eu vou comer {comida} na posição {pos}')
# print(sorted(lanche)) #exibe os itens em ordem alfabética.
# a = (2, 5, 4)
# b = (5, 8, 1, 2)
# c = a+b
# print (c) #quando soma uma tupla a outra, o resultado é a junção das duas tuplas.
# print(len(c)) #mostra a quantidade de elementos da tupla c
# print(c.count(5))#mostra a quantidade de vezes que aparece o numero indicado
# no count dentro da tupla c, neste exemplo o numero 5
# print(c)
# print(c.index(8)) #exibe a posição do numero indicado,
# neste exemplo o numero 8 que ocupa a posição 4. ele pega a primeira posição, caso queira pegar outros deve-se usar o fatiamento de string, partindo do primeiro dado exibido.
#as tuplas podem armazenar tipos diferentes de dados. ex:
# pessoa = ('Danielle', 31, 'F', 64.8)
# del(pessoa) #apaga a tupla da memoria. não é possivel apagar itens da tupla, somente a tupla inteira.
# print(pessoa)



